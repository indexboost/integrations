{
  "name": "functions",
  "description": "IndexBoost render Cloud Function",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "20" },
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^4.0.0",
    "node-fetch": "^2.7.0"
  },
  "private": true
}
